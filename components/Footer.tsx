import React from 'react';

type Props = {};

const Footer = (props: Props) => {
  return (
    <footer className="sticky pb-5 w-full cursor-pointer">
      <img
        className="h-10 w-10 rounded-full grayscale hover:grayscale-0"
        src=""
        alt=""
      />
    </footer>
  );
};

export default Footer;
